#pragma once

namespace rtt {

// The colors of the two teams. Yellow is our default
enum class Team {
    YELLOW, BLUE
};

// Teams expressed relatively.
enum class RelativeTeam {
    US, THEM
};

} // namespace rtt